"use strict";

var _ = {
  // Implements:
  // https://lodash.com/docs#join
  join: (array, separator = ',') => {
    joined = value.join(',')
  }
};


const value = _.join(["hello", "goodbye"], ", ")

console.log(value);